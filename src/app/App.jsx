/**
 * App.jsx — App layer re-export.
 *
 * This file re-exports the root App component from its original location.
 * Future phases will gradually move the implementation here.
 *
 * Consumers should import from '@/app' (the public API):
 *   import { App } from '@/app'
 */

export { default } from '@/App'
