/**
 * AppRouter.jsx — App layer re-export.
 *
 * This file re-exports the AppRouter from its original location in src/router/.
 * Future phases will gradually move the implementation here.
 *
 * Consumers should import from '@/app' (the public API):
 *   import { AppRouter } from '@/app'
 */

export { AppRouter } from '@/router/AppRouter'
