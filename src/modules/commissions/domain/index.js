/**
 * Commissions Module — Domain Layer (re-export)
 *
 * Placeholder: commission domain logic (calculation, lifecycle, status transitions)
 * is embedded in commissionService class methods.
 * Key domain constants:
 *   COMMISSION_RATE = 0.03 (3%)
 *   PAYMENT_DEADLINE_DAYS = 7
 *   MANUAL_UNFREEZE_GRACE_DAYS = 3
 * These constants are module-scoped in commissionService.js and not exported.
 * No domain-specific helpers are exported separately yet.
 */
