/**
 * Commissions Module — Stores Layer (re-export)
 *
 * Placeholder: No dedicated commission store exists.
 * Commission state is managed via local component state (useState) and
 * direct commissionService calls. Auth state (current user) comes from authStore.
 */
