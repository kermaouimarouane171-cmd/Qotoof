/**
 * Commissions Module — Data Layer (re-export)
 *
 * Placeholder: commissionService is the closest to a data layer.
 * Commission data is read/written through Supabase queries in commissionService.
 * No data-layer-specific exports yet.
 */
