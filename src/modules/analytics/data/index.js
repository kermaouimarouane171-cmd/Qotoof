/**
 * Analytics Module — Data Layer (re-export)
 *
 * Placeholder: analyticsApi (in api.js) and reportService are closest to the data layer.
 * Analytics data is fetched through Supabase queries in analyticsApi, reportService,
 * and directly in admin/vendor analytics pages.
 * No data-layer-specific exports yet.
 */
