// ============================================
// Marketplace Module — UI Public API
// Re-exports marketplace browsing pages and components.
// No files were moved — this is a re-export layer.
// ============================================

// Marketplace product listing page
export { default as MarketplacePage } from '@/pages/Marketplace'

// Stores listing page
export { default as StoresPage } from '@/pages/Stores'

// Store detail page
export { default as StoreDetailPage } from '@/pages/StoreDetail'

// Search results page
export { default as SearchResultsPage } from '@/pages/SearchResults'

// Seasonal calendar page
export { default as SeasonalPage } from '@/pages/Seasonal'

// Search bar component (reusable marketplace search widget)
export { default as SearchBar } from '@/components/Search/SearchBar'
