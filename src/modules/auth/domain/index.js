// ============================================
// Auth Module — Domain Public API
// Re-exports auth domain constants and rules.
// No files were moved — this is a re-export layer.
// ============================================

// Role definitions and RBAC
export { USER_ROLES, ROLE_PERMISSIONS, PUBLIC_PATHS } from '@/constants/roles'
