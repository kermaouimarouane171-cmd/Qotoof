/**
 * Coupons Module — Hooks Layer (re-export)
 *
 * Placeholder: No coupon-specific React Query hooks exist yet.
 *
 * Coupon data fetching is currently done via direct couponsApi calls
 * in pages (CheckoutSimplified.jsx, buyer/Coupons.jsx, vendor/Coupons.jsx).
 * Future: create useCoupons, useVendorCoupons, useCouponValidation hooks
 * and re-export them from here.
 */
