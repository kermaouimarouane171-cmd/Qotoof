// ============================================
// Catalog Module — Hooks Public API
// Re-exports product-related React hooks.
// No files were moved — this is a re-export layer.
// ============================================

export {
  useProducts,
  useAvailableRegions,
  useProductById,
  productsQueryKey,
  productQueryKey,
} from '@/hooks/useProducts'
