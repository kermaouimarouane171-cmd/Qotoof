/**
 * Admin Module — Stores Layer (re-export)
 *
 * Placeholder: No dedicated admin store exists.
 * Admin state relies on:
 *   - authStore (useAuthStore) — for current user, profile, role, auth state
 *   - Local component state (useState) — for admin page-specific state
 *   - React Query cache — for server state (users, stats, etc.)
 */
