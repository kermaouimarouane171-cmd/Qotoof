// ============================================
// Shared Module — Hooks Public API
// Re-exports only generic, framework-level hooks
// that have no Supabase or business-logic dependencies.
// ============================================

export { useModal } from '@/hooks'
export { usePagination } from '@/hooks'
export { useToast } from '@/hooks'
export { useClipboard } from '@/hooks'
export { useLocalStorage } from '@/hooks'
export { useMediaQuery } from '@/hooks'
export { useBreakpoint } from '@/hooks'
export { useInterval } from '@/hooks'
export { useDocumentTitle } from '@/hooks'
export { useConfirmation } from '@/hooks'
