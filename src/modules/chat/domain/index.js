/**
 * Chat Module — Domain Layer (re-export)
 *
 * Placeholder: chat domain logic (conversation creation, message validation,
 * read status logic) is embedded in chatService class methods.
 * No domain-specific helpers are exported separately yet.
 */
