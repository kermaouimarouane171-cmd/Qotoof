/**
 * Chat Module — Stores Layer (re-export)
 *
 * Placeholder: No dedicated chat store exists.
 * Chat state is managed via React Query cache, local component state,
 * and authStore (for current user identity).
 */
